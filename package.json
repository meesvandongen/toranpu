{
  "name": "patience",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --parallel",
    "lint": "turbo run lint"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/meesvandongen/patience.git"
  },
  "devDependencies": {
    "turbo": "^1.6.1"
  }
}
