{
  "name": "toranpu-repo",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --parallel",
    "lint": "turbo run lint",
    "docs": "turbo run docs",
    "publish-packages": "turbo run docs build lint test && changeset version && changeset publish"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/meesvandongen/patience.git"
  },
  "devDependencies": {
    "@changesets/cli": "^2.26.0",
    "turbo": "^1.7.0"
  }
}
